// string length find out use built in function in c program...
#include <stdio.h>
#include <string.h>

int main() {
    char name[] = "My Country is Bangladesh";
    int len = strlen(name);
    printf("String length is: %d", len);

return 0;
}
